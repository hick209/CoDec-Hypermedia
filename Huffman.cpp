#include "Huffman.h"

#include <stdio.h>
#include "Utils.h"


void huffmanCompress(const char* filename, const char* filenameExtensionLess) {
    FILE* file = fopen(filename, "r");

    // TODO

    fclose(file);
}

